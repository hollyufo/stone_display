# How to run

```
   - install composer
   - install node js
   - run terminal in app directory
   - run composer install
   - run npm install
   - run npm run dev
   - copy .env.exemple content to .env and fill required data
   - php artisan migrate
   - php artisan serve
```